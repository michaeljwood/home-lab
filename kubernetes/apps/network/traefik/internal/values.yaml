## File values.yaml ##

ingressClass:
  name: "traefik-internal"
  isDefaultClass: false
gatewayClass:
  name: "traefik-internal"

providers:
  kubernetesIngress:
    enabled: true
  kubernetesGateway:
    enabled: true

gateway:
  namespacePolicy: All

logs:
  access:
    enabled: true
